import { BaseAudioContext } from '../api';
import AudioWorkletProcessor from './AudioWorkletProcessor';
export interface AudioWorkletOptions {
    outputChannelCount: number[];
    numberOfInputs: number;
    numberOfOutputs: number;
    name: string;
    processorCtor: typeof AudioWorkletProcessor;
}
export declare function getWorkletProcessor(audioContext: BaseAudioContext, name: string): typeof AudioWorkletProcessor | undefined;
export default class AudioWorklet {
    private readonly audioContext;
    constructor(audioContext: BaseAudioContext);
    addModule(_url: string, options?: AudioWorkletOptions): Promise<null>;
}
