/// <reference types="node" />
import type { MessagePort } from 'worker_threads';
import { AudioWorkletOptions } from './AudioWorklet';
export interface AudioParamDescriptor {
    name: string;
    automationRate?: 'a-rate' | 'k-rate';
    minValue?: number;
    maxValue?: number;
    defaultValue?: number;
}
export default class AudioWorkletProcessor {
    port: MessagePort;
    sampleRate: number;
    currentTime: number;
    constructor(_options: Partial<AudioWorkletOptions>);
    process(_input: Float32Array[][], _output: Float32Array[][], _parameters: Record<string, Float32Array>): void;
    static parameterDescriptors: AudioParamDescriptor[];
}
